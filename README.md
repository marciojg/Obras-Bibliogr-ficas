# Obras Bibliográficas

- Docker instalado

Subir a aplicação

```bash
docker-compose up
```

Rodando teste
```bash
docker-compose up
docker-compose exec application bash -c "rails db:migrate && rails test"
```